# The stack targets Node 20 LTS; do not hard-fail installs on newer local Node.
engine-strict=false
# Keep peer deps auto-installed for a predictable first install.
auto-install-peers=true
# Hoist just enough for tooling (eslint/prettier/turbo) to resolve at the root.
shamefully-hoist=false
